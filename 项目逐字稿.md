## AI对话平台

本文按照 STAR（Situation、Task、Action、Result）规则整理项目亮点，方便在面试或产品文档中展示技术要点与工程价值。

### 亮点一：大文件分片上传与 MD5 校验（STAR）

- **Situation（背景）：** 浏览器需稳定上传几十兆到百兆级文档，网络不可控、设备内存有限，且上传文件将作为大模型问答的知识源，需保证完整性。
- **Task（目标）：** 设计一套前后端协作的分片上传方案，支持断点续传、分片与整文件校验、可视化进度与资源安全管理。
- **Action（方案与实现）：**
  - 前端将文件按固定大小（例如 ~1MB）分片，分片内计算校验值并在本地分片式读取以避免大内存占用；
  - 发起初始化请求上传文件元信息（文件指纹、分片总数等），后端返回上传任务 ID 和已存在分片列表；
  - 采用受控并发（小批次并发，如 3 并发）上传分片，利用浏览器上传进度事件采集每片进度，仅重试失败分片；
  - 后端按序保存分片为临时文件、逐片校验并记录成功列表；全部分片就绪后按序合并，并对合并后的完整文件做整体指纹校验；合格后才对外提供访问地址；
  - 合并期间及时删除临时分片、对异常任务定期清理，避免磁盘泄露。
- **Result（成果与价值）：**
  - 显著降低弱网重传成本，支持断点续传与节省流量；
  - 通过分片与整包双重校验保证落盘文件与用户本地一致性；
  - 为将上传文档作为模型知识源提供了可信的数据基础。

### 亮点二：基于 Zustand 的多 store 状态管理（STAR）

- **Situation（背景）：** 前端需同时管理会话列表、消息流、输入框、上传进度、主题等多类状态，若集中管理易造成耦合与过度重渲染。
- **Task（目标）：** 构建轻量、按领域拆分的状态系统，支持按需订阅、局部持久化与高性能渲染控制。
- **Action（方案与实现）：**
  - 将状态划分为会话、消息、文件上传、主题等独立 store，每个 store 封装状态与业务动作（增删改、切换、持久化等）；
  - 组件使用 Zustand 选择器按需订阅只关心的字段，避免无关更新导致重渲染；对需要跨会话持久化的数据（如会话列表、主题）写入本地存储，聊天记录使用persist中间件持久保存，防止刷新丢失，运行时临时数据（如 SSE 控制器、上传中进度）仅保留内存中；
  - 维护单向依赖：会话为基础，消息基于会话，上传可被消息查询但与消息解耦。
- **Result（成果与价值）：**
  - 降低无关组件重渲染、提升性能；
  - 提高模块可维护性与演进效率；
  - 调试便利，可单独验证各 store 行为。

### 亮点三：基于 SSE 的实时 AI 对话（STAR）

- **Situation（背景）：** 大模型推理耗时且不稳定，用户期望流式、打字机式的实时回复展示。
- **Task（目标）：** 实现前后端的流式通信，支持增量显示、取消生成、超时与错误处理。
- **Action（方案与实现）：**
  - 前端在发送消息时插入生成中占位消息，使用 `fetch-event-source` 或 SSE 客户端保持长连接，接收增量 token 并追加到占位消息，绑定中断控制器支持用户取消；
  - 后端提供桥接接口，将上游模型的流式输出解析为 SSE 事件推送至前端，设置合适的响应头（事件流、禁用缓存、关闭代理缓冲）以保证低延迟转发；
  - 在首页聊天页场景采用先写入全局消息、聊天页自动接力触发生成的模式，避免重复请求并确保 UX 顺滑；加入超时保护与异常通知逻辑。
- **Result（成果与价值）：**
  - 用户获得实时、增量的生成体验（打字机效果）；
  - 支持中途停止、超时與错误提示，避免长期挂起；
  - 资源在客户端断开时及时释放，降低后端成本。

### 亮点四：高性能聊天体验（虚拟滚动、懒加载等）（STAR）

- **Situation（背景）：** 长对话与大量图片会导致 DOM 爆炸、带宽与内存压力以及滚动卡顿。
- **Task（目标）：** 在保证自然交互的前提下，显著降低渲染成本、优化图片加载与控制高频事件。
- **Action（方案与实现）：**
  - 虚拟滚动：采用 `react-window` 或等效方案，仅渲染可视区消息，结合高度估算与上下缓冲避免白屏；
  - 图片懒加载：使用 Intersection Observer 在进入缓冲区前触发下载，远端资源仅渲染占位；
  - 组件记忆化：对消息单元和静态组件进行 memo 化，减少重复渲染；
  - 高频事件控制：对滚动使用节流、对输入同步使用防抖，新增消息时根据用户是否在底部决定是否自动滚动。
- **Result（成果与价值）：**
  - 在长对话场景保持流畅滚动，显著减少布局与重绘开销；
  - 降低带宽与内存浪费，提升移动端首屏体验；
  - 用户体验自然，无需额外学习成本。

### 总结与关键成果

- 该项目在大文件上传、前端状态管理、实时流式对话与界面性能四个方面形成了完整工程实践：
  - 分片 + 双重校验保证文件完整性与可恢复上传；
  - 多 store 的 Zustand 架构降低耦合、提升性能与可维护性；
  - SSE 流式转发实现打字机式实时交互并支持终止与超时控制；
  - 虚拟滚动与懒加载确保在海量消息下仍然流畅。

请确认是否需要我：
- 将原文的具体实现代码片段（例如 SparkMD5 使用示例、后端合并逻辑伪码、SSE 桥接示例）提取并追加到文档中；
- 或把本文件转成中文版面试稿（精简版）或英文版。
